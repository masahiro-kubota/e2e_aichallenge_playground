# @package _global_
# Evaluation Configuration

defaults:
  - base
  - _self_

ad_components: lateral_shift

experiment:
  name: "evaluation"
  type: "evaluation"
  description: "Closed-loop evaluation of trained model"
