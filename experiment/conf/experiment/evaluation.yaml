# @package _global_
# Evaluation Configuration

defaults:
  - base
  - _self_

agent: tiny_lidar

experiment:
  name: "evaluation"
  type: "evaluation"
  description: "Closed-loop evaluation of trained model"
